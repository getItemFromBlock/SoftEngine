#pragma once
// TODO:
/*
#include <filesystem>

class ResourceManager;

namespace ofbx
{
    struct IScene;
    struct Material;
}
class Model;

class FBXLoader
{
public:
    static void Load(const std::filesystem::path& fullPath, Model* outputModel, ResourceManager* resourceManager);
private:
    static void LoadTextures(ofbx::IScene* fbxScene, const std::filesystem::path& fullPath, ResourceManager* resourceManager);
    static void LoadModel(ofbx::IScene* fbxScene, const std::filesystem::path& fullPath, Model* outputModel, ResourceManager* resourceManager);

};
*/